# Jingdu v2.3.8 Source Release

version: v2.3.8
kind: source-release
google_play_production: false

This patch release advances Jingdu from v2.3.7 after the bounded Reader page-turn hot-path optimization completed its fail-closed qualification chain. It does not add TalkBack or unrelated product work, change benchmark definitions, relax performance thresholds/baselines/sample floors, alter Baseline/Startup Profile generation, or claim Google Play production qualification.

The exact pre-release `main` SHA is `acef778ca6b60e2ec0fa7d84e37518599d7f5506`. It is the normal-merge result of PR #103, whose exact head `d0a35e60a5064668a8da8ef79da730f3fd7f67e3` changed exactly one production file after CI Contracts #132, canonical CI #1484, and Reader Performance PR #73 all passed on the first attempt with the existing gates unchanged.

That exact `main` SHA passed canonical CI #1485 / run `34768030351`. API 36 / PAGE_SIZE=16384 functional instrumentation passed 29/29 tests, and the independent 200% font-scale `JingduUiTest` suite passed 15/15 tests. Functional evidence artifact `10321536043` has SHA256 `738a2f04fc74cf56ea0ba94ee59840c89257665baddb78da2c7d54f3079f551f`.

Hosted Reader performance on the exact pre-release main passed all unchanged SLOs: page-turn P95/P99 `63.809/75.425ms` against effective gates `74.000/86.284ms`; continuous-scroll P95/P99 `47.932/60.231ms` against `148.199/178.639ms`; Chapters/Settings P95/P99 `91.111/107.597ms` against `155.359/215.882ms`. R8 Macrobenchmark completed 5/5, Baseline Profile generation completed 2/2, and canonical baseline/startup profiles contain `24363 / 22983` rules. Performance evidence artifact `10321851867` has SHA256 `f7762e1af7fba62b94042f94e1172703f697a74ca111d113c5e745ff1af6122a`.

The exact-main 16 KiB release packaging gate also passed: relevant arm64-v8a and x86_64 ELF LOAD alignments are `0x4000`, pinned NDK is `29.0.14206865`, and the Release AAB plus native-debug-symbols output were generated successfully.

Repository-side Play/privacy source contracts remain fail closed, but real Google Play production qualification remains external. Production/upload signing, production AAB provenance, Play app-bundle/pre-launch validation, physical API/OEM/accessibility/performance evidence, Billing license-tester evidence, Play Console Data Safety/policy submission, store asset upload, and staged rollout/vitals evidence are not claimed by this source release.

This release-only branch changes exactly the Jingdu Android default versionCode from 13 to 14, versionName from 2.3.7 to 2.3.8, root Android releaseVersion from 2.3.7 to 2.3.8, and this source-release manifest. After fresh exact-head PR CI and Reader Performance pass, it may be normal-merged with its exact expected head. The resulting exact-main release CI must then prepare the annotated `v2.3.8` tag and draft GitHub Release, attach the repository-stable debug-signed APK plus `SHA256SUMS.txt` and `SIGNING-CERT-SHA256.txt`, and only then may Finalize Immutable Release publish `v2.3.8` as non-draft, non-prerelease and immutable at the exact gated release merge SHA.
