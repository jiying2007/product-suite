# Store asset ownership

The existing `store/play/` files are Jingdu Google Play planning/listing assets retained at their stable paths. They are not a shared store namespace for every product.

Future products must own product-scoped store material, for example `store/audiolab/play/`, and must not inherit Jingdu claims, screenshots, SKUs, privacy statements or rollout assumptions by convenience.
