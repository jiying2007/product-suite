# AI Platform

Optional shared AI runtime boundary for model/provider routing, local inference, cloud inference, quotas, and evaluation hooks when more than one product needs them.

AI is a capability, not the repository identity. Products that do not need AI must not depend on this layer.
