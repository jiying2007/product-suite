# Audio Platform

Shared audio capabilities live here only after at least two products require the same stable contract. Expected candidates include capture/playback abstractions, DSP primitives, measurement utilities, codecs, and test fixtures.

Do not move product-specific AudioLab or Voice Cleaner behavior here merely to make the tree look shared.
