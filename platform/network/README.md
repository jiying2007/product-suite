# Network Platform

Shared network capabilities live here only when multiple products consume the same stable contract. Candidate layers include address/subnet utilities, protocol parsing, diagnostics primitives, and reusable test fixtures.

Android permission/UI policy and product-specific scan behavior remain in the consuming product.
