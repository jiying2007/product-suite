# Quality-data ownership

The existing `quality/smartclean/` corpus belongs to Jingdu Smart Clean and is retained at its stable path for reproducible quality gates. It is not a generic training/evaluation bucket for future products.

New products must use product-scoped quality namespaces such as `quality/audiolab/` only after they have concrete product-owned datasets, evaluation contracts and privacy rules.
