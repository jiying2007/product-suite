# Copy to ignored keystore.properties only after the release owner creates an upload key.
# Keep the real file and keystore out of source control and use an absolute storeFile path.
storeFile=/absolute/path/to/jingdu-upload-key.jks
storePassword=REPLACE_LOCALLY
keyAlias=REPLACE_LOCALLY
keyPassword=REPLACE_LOCALLY
