# Copy to keystore.properties outside version control for release qualification.
# Pose Studio must own a product-specific upload/release key; do not reuse another product identity.
storeFile=/absolute/path/to/pose-studio-upload.jks
storePassword=change-me
keyAlias=pose-studio
keyPassword=change-me
